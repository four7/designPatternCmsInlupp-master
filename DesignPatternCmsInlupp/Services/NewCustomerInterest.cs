using DesignPatternCmsInlupp.Models;
using DesignPatternCmsInlupp.Services;
using DesignPatternCmsInlupp.Controllers;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace DesignPatternCmsInlupp.Services
{
    //public class NewCustomerInterest : IInterestRate
    //{
    //    public decimal Interest(CallInstructions model)
    //    {
    //        var c = FindCustomer(model.Personnummer);
    //        model.Result = true;
    //        if (c == null)
    //            model.Customer = c;

    //        int age = GetAge(model.Personnummer);
    //        decimal baseRate = .GetRiksbankensBaseRate();

    //        if (age < 18)
    //            model.RateWeCanOffer = 30.22m + baseRate;
    //        else if (age < 35)
    //            model.RateWeCanOffer = 32.18m + baseRate;
    //        else if (age < 65)
    //            model.RateWeCanOffer = 22.30m + baseRate;
    //        else
    //            model.RateWeCanOffer = 45.30m + baseRate;

    //        return model;
    //    }

    //}
}